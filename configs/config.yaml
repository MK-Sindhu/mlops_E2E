# Project Configuration
project:
  name: "credit-card-fraud-detection"
  version: "1.0.0"

# Data settings
data:
  raw_path: "data/raw/creditcard.csv"
  processed_path: "data/processed/"
  baselines_path: "data/baselines/"
  test_size: 0.2
  random_state: 42

# Feature engineering
features:
  # Features to keep from original dataset
  drop_columns: ["Time"]
  target_column: "Class"
  # Scaling
  scale_columns: ["Amount"]

# Model settings
model:
  algorithm: "xgboost"
  params:
    n_estimators: 100
    max_depth: 6
    learning_rate: 0.1
    scale_pos_weight: 50  # Handle class imbalance
    eval_metric: "aucpr"
    random_state: 42
  # Optimization for local hardware
  optimization:
    quantize: true       # When true, force tree_method=hist + reduced max_bin
    max_bin: 128         # Histogram bins (XGBoost default is 256)
    n_jobs: -1

# MLflow settings
mlflow:
  # Default to local file store so training works without a running server.
  # Override with env var: MLFLOW_TRACKING_URI=http://mlflow:5000
  tracking_uri: "file:./mlruns"
  experiment_name: "fraud-detection"
  registered_model_name: "fraud-detection-xgboost"

# API settings
api:
  host: "0.0.0.0"
  port: 8000
  model_path: "models/best_model.joblib"

# Monitoring
monitoring:
  drift_threshold: 0.05  # KS-test p-value threshold
  performance_decay_threshold: 0.80  # Minimum F1-score
  alert_error_rate: 0.05  # 5% error rate threshold
  feedback_window: 100  # Evaluate after N feedback entries

# Security
security:
  encrypt_data_at_rest: true
  encryption_key_path: "configs/.encryption_key"
